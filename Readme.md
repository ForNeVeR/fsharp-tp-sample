F# Type Provider sample
=======================

How to run
----------

```console
$ ./bootstrap.ps1 # downloads Paket and sets the dependencies up
$ dotnet build # builds the type provider
$ fsi ./example.fsx # uses the type provider
```
